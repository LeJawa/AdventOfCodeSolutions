{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run",
            "type": "shell",
            "command": "python ${file}",
            "options": {
                "env": {
                    "PYTHONPATH": "${workspaceFolder}"
                }
            },
        }
    ]
}